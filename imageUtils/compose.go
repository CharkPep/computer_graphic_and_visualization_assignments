package imageUtils

import (
	"image"
)

// Compose Method to compose an image with another image
func (Img *ProcessableImage) Compose(image *image.Image, x, y int) {
	//bounds := (*Img.Img).Bounds()
	//newImage := image.NewRGBA(bounds)
}
